//import { parseISO, format } from 'date-fns'

export default function Date({ dateString }) {
  const date ='2020-10-10';// parseISO(dateString)
 /*  return <time dateTime={dateString}>{format(date, 'LLLL	d, yyyy')}</time> */
return <p>{date}</p>
}
